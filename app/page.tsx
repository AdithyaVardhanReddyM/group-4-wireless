"use client";
import ThreeScene from "@/components/ThreeScene";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <ThreeScene />
    </div>
  );
};

export default page;
